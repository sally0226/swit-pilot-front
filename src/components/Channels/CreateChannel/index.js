import React from 'react';
import { Container, Input, Title } from './style';

const CreateChannel = () => {
  return (
    <Container>
      <Title>채널 이름</Title>
      <Input />
    </Container>
  );
};

export default CreateChannel;
